
# Virtual environment placeholder

This directory is a placeholder only. Create your local virtual environment here if you want to keep it inside the repository:

```bash
python -m venv venv
source venv/bin/activate
```

The environment itself is ignored by git.
